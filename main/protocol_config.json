{
  "protocols": [
    {
      "protocol": "uart",
      "inputs": ["clk", "rx"],
      "outputs": ["tx", "tx_out"],
      "parameters": ["baud_rate", "clk_frequency"]
    },
    {
      "protocol": "spi",
      "inputs": ["clk", "miso"],
      "outputs": ["mosi", "cs","sckl"],
      "parameters": []
    }
    
  ]
}
